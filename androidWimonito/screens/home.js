import React from 'react';

import {
    ScrollView,
    View,
    Text
} from 'react-native';

const Home = ({ navigation }) =>{
    return(
        <ScrollView>
            <View>
                <Text>
                    Home
                </Text>
            </View>
        </ScrollView>
    )
}

export default Home;
